<h1> Create a new task </h1>

<br>
<%= render partial: "layouts/errors", locals: {object: @task} %>

<%= form_for(@task) do |f| %>

    <%= f.label :content %>
    <%= f.text_area :content %>

    <%= f.submit %>

<% end %>